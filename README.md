# SillowApp

## Setup

This app requires a Google Maps API key.

1. Get an API key from Google Cloud Console.
2. Open `Resources/Raw/Data/maps.html`.
3. Replace `YOUR_GOOGLE_MAPS_API_KEY` with your actual API key.
4. Build and run the app.
